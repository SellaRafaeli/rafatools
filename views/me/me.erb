<h1> Me </h1>

<div>
	<h4>Your phone number: <%= cu['num'] %></h4>
	
	<% if false %>	
	<h4>Your account is active: <span class='active'> <%= !!cu['active'] %> </span> <button onclick=toggleActive()> Toggle </button></h4>
	<% end %>

	<% if !cu['paid'] %>	
	<h4> Pay to start getting messages: <%= erb :'other/stripe' %> </h4>
	<% else %>
		<h4> You will get a helpful message approximately once every 3 hours, between 10:00 and 22:00.</h4>
		<form action='me' method='post'>
			<label> Your GMT offset: <input name=gmt_offset type=number value="<%=cu[:gmt_offset]%>" min=-12 max=12 style='width: 50px'/> </label>
			<br/>
			<label> Receive messages: <input name=active type=checkbox <%="checked" if cu[:active]%> /> </label>
		<!-- <label> You will receive messages </label>

		<label> every </label>
		<select name='start_hour'>
			<% (1..23).to_a.each do |i| %>
				<option><%= i %></option>
			<% end %>
		</select>
		
		<label> from </label>
		<select name='start_hour'>
			<% (1..23).to_a.each do |i| %>
				<option><%= i %></option>
			<% end %>
		</select>

		<label> until </label>
		<select name='end_hour'>
			<% (1..23).to_a.each do |i| %>
				<option><%= i %></option>
			<% end %>
		</select> -->
			<br/>
			<button> Update </button>
		</form>
	<% end %>

	<a href='/logout' class='btn btn-primary btn-raised'> Log out </a>
	

</div>

<script>
function toggleActive() {
	$.post('/me/toggle_active').then(res => { 
		console.log(res);
		$('.active').text(res.active);
	});
}
</script>